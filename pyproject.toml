[project]
name = "spicy-format"
version = "0.27.0"
license = { file = "LICENSE" }
readme = "README.md"

[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[tool.maturin]
bindings = "bin"
