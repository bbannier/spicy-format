[project]
name = "spicy-format"
version = "0.27.1"
license = { file = "LICENSE" }
readme = "README.md"
requires-python = ">=3.8"

[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[tool.maturin]
bindings = "bin"
