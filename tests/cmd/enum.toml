stdin = """
public type E = enum {
A , # A.
B, C};

type Priv11 = enum {A,B,C};
type Priv12 = enum {A,B,C};

type E = enum {
    A = '1',
    B = '2',
    C = '3',
    D = '4',
    E = '5',
    F = '6',
};
"""

stdout = """
public type E = enum {
    A, # A.
    B,
    C,
};

type Priv11 = enum {
    A,
    B,
    C,
};
type Priv12 = enum {
    A,
    B,
    C,
};

type E = enum {
    A = '1',
    B = '2',
    C = '3',
    D = '4',
    E = '5',
    F = '6',
};
"""

stderr = ""
status.code = 0
